name: CI con AutoCommit

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

permissions:
  contents: write   # 👈 IMPORTANTE: permite que el bot pueda hacer commits/push


jobs:
  test-and-update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Configurar Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Instalar dependencias
        run: pip install pytest

      - name: Ejecutar script de tests y actualizar README
        run: python update_readme.py

      - name: Commit automático del README
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "Update README y report con estado de tests"
          file_pattern: README.md report.md test-report.md
          
[![Badge showing status of CI con AutoCommit workflow for main branch. Text reads CI con AutoCommit. Badge color and text indicate current workflow status. Located within GitHub repository DE-2526_DAW_u1_action-SDUDAW.](https://github.com/sdurutr436/DE-2526_DAW_u1_action-SDUDAW/actions/workflows/ci.yaml/badge.svg?branch=main)](https://github.com/sdurutr436/DE-2526_DAW_u1_action-SDUDAW/actions/workflows/ci.yaml)
